"use client";

// Font files can be colocated inside of `pages`b
export default function Instructions() {
  return (
    <main className="h-[calc(100vh-6rem)] bg-background px-4 font-inter text-primary"></main>
  );
}
